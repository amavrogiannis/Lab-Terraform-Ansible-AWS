- name: Setup Webserver - Ansible Demo @ AND
  hosts: tags_alex_mavrogiannis # tag_Contact_alex_mavrogiannis
  become: yes
  # gather_facts: no
  become_method: sudo

  roles:
    - apache 
    - applications
    - container